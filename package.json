{
  "name": "tsuke",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve src/index.pug --out-dir dist",
    "build": "parcel build src/index.pug --out-dir docs",
    "deploy": "gh-pages -d docs"
  },
  "dependencies": {
    "pts": "^0.6.6"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "node-sass": "^4.9.4",
    "parcel-bundler": "^1.10.3",
    "pug": "^2.0.3",
    "typescript": "^3.1.3"
  }
}
